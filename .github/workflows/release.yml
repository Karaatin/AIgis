name: Release AIgis Chrome Extension

on:
  push:
    tags:
      - 'v*'

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write
  pages: write
  id-token: write

jobs:

  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'

      - name: Install Dependencies
        run: npm ci

      - name: Run Tests
        run: npm run test -- --run

      - name: Verify manifest version matches tag
        run: |
          MANIFEST_VERSION=$(node -p "require('./public/manifest.json').version")
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          if [ "$MANIFEST_VERSION" != "$TAG_VERSION" ]; then
            echo "::error::Tag $GITHUB_REF_NAME does not match manifest version $MANIFEST_VERSION"
            exit 1
          fi

      - name: Build Extension
        run: npm run build

      - name: Check Build Output
        run: ls -R dist

      - name: Create Zip
        run: |
          cd dist
          zip -r ../AIgis-${{ github.ref_name }}.zip .
          cd ..
      
      - name: Generate Checksum
        run: sha256sum AIgis-${{ github.ref_name }}.zip > AIgis-${{ github.ref_name }}.zip.sha256

      - name: List Artifacts
        run: ls -l AIgis-${{ github.ref_name }}.*

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            AIgis-${{ github.ref_name }}.zip
            AIgis-${{ github.ref_name }}.zip.sha256
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

